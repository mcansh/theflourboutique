extends layout

block back-to-home
  a.back-to-home(href="/") Back to home
block content
  != h.icon('logo')
  <!--[if IE 8]><div class="logo"></div><![endif]-->
    
  h1 Cookie Order Quote
  form(method="POST" action="/order")
    label(for="name") Name:
    input#name(type="text" name="name" placeholder="Name" required)
    label(for="email") Email:
    input#email(type="email" name="email" placeholder="Email" required)
    label(for="city") City:
    input#city(type="text" name="city" placeholder="City" required)
    label(for="date") Date of Event:
    input#date(type="date" name="date" placeholder="MM/DD/YYYY" required)
    label(for="theme") Theme:
    input#theme(type="text" name="theme" placeholder="Ex: wedding shower, baby shower, birthday" required)
    label(for="quantity") Quantity:
    input#quantity(type="number" name="quantity" placeholder="Quantity (minumum order 12 cookies)" min="12" required)
    label(for="flavor") Flavor:
    - const flavors = ['Vanilla sugar cookie with almond royal icing', 'Chocolate cookie with vanilla royal icing', 'Lemon poppyseed sugar cookie with lemon royal icing', 'Lemon sugar cookie with raspberry royal icing', 'Cookies and Cream sugar cookie with Cookies and Cream royal icing', 'Mint Chocolate Chip cookie with mint royal icing']
    select(id="flavor" name="flavor" required)
      option(selected disabled) Flavor of Cookies
      each flavor in flavors
        option= flavor
    
    label(for="comments") Comments
    textarea#comments(name="comments")

    input.btn(type="submit" value="Submit")
    
  .notes
    p Must give at least 3 weeks notice. I book up fast so please order early.
    p I do not ship cookies. I will drive 5 miles to deliver for free. If you live further, we could meet half way.
    p Normal response time is 24-48 hours.
